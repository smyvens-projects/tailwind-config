import { Config } from "tailwindcss";

const TailwindConfig: Config = {
  content: [],
  darkMode: "class",
  theme: {
    extend: {
      colors: {
        primary: "#eab208",
      },
    },
  },
};

export = TailwindConfig;
